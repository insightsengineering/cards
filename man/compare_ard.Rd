% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_ard.R
\name{compare_ard}
\alias{compare_ard}
\title{Compare ARDs}
\usage{
compare_ard(
  x,
  y,
  keys = c(all_ard_groups(), all_ard_variables(), any_of(c("variable", "variable_level",
    "stat_name"))),
  compare = any_of(c("stat_label", "stat", "stat_fmt")),
  tolerance = sqrt(.Machine$double.eps),
  check.attributes = TRUE
)
}
\arguments{
\item{x}{(\code{card})\cr
first ARD to compare.}

\item{y}{(\code{card})\cr
second ARD to compare.}

\item{keys}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
columns identifying unique records. The intersection of the selected
columns in both ARDs is used. Default is
\code{c(all_ard_groups(), all_ard_variables(), any_of(c("variable", "variable_level", "stat_name")))}.}

\item{compare}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
columns to compare between the two ARDs. Default is
\code{any_of(c("stat_label", "stat", "stat_fmt"))}.}

\item{tolerance}{(\code{numeric(1)})\cr
numeric tolerance passed to \code{all.equal()} for numeric comparisons.
Default is \code{sqrt(.Machine$double.eps)}.}

\item{check.attributes}{(\code{logical(1)})\cr
logical passed to \code{all.equal()} indicating whether object attributes
(e.g. names) should be compared. Default is \code{TRUE}.}
}
\value{
a named list of class \code{"ard_comparison"} containing:
\itemize{
\item \code{rows_in_x_not_y}: data frame of rows present in \code{x} but not in \code{y}
(based on key columns)
\item \code{rows_in_y_not_x}: data frame of rows present in \code{y} but not in \code{x}
(based on key columns)
\item \code{compare}: a named list where each element is a data frame containing
the key columns, the compared column values from both ARDs, and a
\code{difference} column with the \code{all.equal()} description for rows where
values differ
}
}
\description{
\code{compare_ard()} compares columns of two ARDs row-by-row using a shared set
of key columns. Rows where the column values differ are returned.
}
\examples{
base <- ard_summary(ADSL, by = ARM, variables = AGE)
compare <- ard_summary(dplyr::mutate(ADSL, AGE = AGE + 1), by = ARM, variables = AGE)

compare_ard(base, compare)$compare$stat

}
